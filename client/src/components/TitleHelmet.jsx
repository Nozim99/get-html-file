import {Helmet} from "react-helmet-async";

const TitleHelmet = () => {
    return (
        <Helmet>
            <title>HTML & CSS & JS file</title>
        </Helmet>
    );
};

export default TitleHelmet;